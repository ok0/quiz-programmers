package challenges.lv2.menu_renewal

class Solution {
  fun solution(orders: Array<String>, course: IntArray): Array<String> {
    val o = mutableListOf<HashMap<String, Int>>()

    course.forEach { c ->
      orders.forEach { order ->
        var startIndex = 0
        for (index in (startIndex+1) .. order.lastIndex) {

        }
      }
    }


    return arrayOf()
  }
}